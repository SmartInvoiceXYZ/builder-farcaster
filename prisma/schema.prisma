datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Cache {
  key       String   @id
  value     String
  timestamp DateTime
}
